.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install sclust

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv sclust
    $ pip install sclust
